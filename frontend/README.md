# Uni-Connect Frontend

This merged frontend keeps the original `frontend` folder structure and runs as one Vite app.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run preview`

## Structure

- Public society pages: `src/pages/*.jsx`
- Student and admin portal pages: `src/pages/auth`, `src/pages/student`, `src/pages/admin`
